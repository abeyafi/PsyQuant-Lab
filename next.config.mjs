/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'picsum.photos',
      },
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
      {
        protocol: 'https',
        hostname: 'images.remote-host.com',
      },
    ],
  },
  // Ensure the app doesn't hang during static generation by setting a timeout or disabling it for problematic routes if needed
  // staticPageGenerationTimeout: 120,
};

export default nextConfig;
